"""
DEAD FILE — Elasticsearch has been removed from this stack.

All callers have been migrated:
  services/emissions.py  → stubs pending Postgres (calculated_emissions)
  services/anomaly.py    → stubs pending Postgres anomaly table
  services/uploads.py    → parse-only; indexing pending ActivityRecord pipeline

This file and the elasticsearch package dep can be deleted once the new pipeline is live.
"""
