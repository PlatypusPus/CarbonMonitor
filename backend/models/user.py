"""User & Role SQLAlchemy models — placeholder.

Phase: Database models. Tables: users, roles, sessions (refresh-token store).
"""

# SQLAlchemy User/Role/Session models are defined in the Database models phase.
